/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname:"jpighzqdrkqxike6.public.blob.vercel-storage.com",
            },
        ],
     },
};

module.exports = nextConfig
